# This file is maintained for backward compatibility
# It is recommended to install the package directly:
# pip install -e .
#
# or with development dependencies:
# pip install -e ".[dev]"

ffmpeg-python>=0.2.0
tqdm>=4.66.4
aioboto3>=11.0.0
